{
  "model": "golden-test-model",
  "description": "Try to read non-existent file, get error, then search instead",
  "responses": [
    "Let me read the report file.\nrun(command=\"files.read path=report.txt\")",
    "The file doesn't exist. Let me search for any report files.\nrun(command=\"files.search query=report include=*.txt\")",
    "Found report files in the directory.\n<done>"
  ]
}
